---
phase: 02-sqlite-catalog
plan: 03
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/lib/catalog-import.ts
  - src/lib/catalog.ts
  - src/app/api/sync-hook/route.ts
  - scripts/rebuild-catalog.ts
  - scripts/nightly-insights.ts
  - data/catalog/last-import-validation.json
  - src/lib/__tests__/catalog-parity.test.ts
  - src/lib/__tests__/catalog-import-validation.test.ts
  - src/lib/__tests__/catalog-cache.test.ts
  - README.md
  - docs/architecture/system-overview.md
autonomous: true
requirements:
  - PERF-04
  - SAFE-04
  - TEST-03
user_setup: []
must_haves:
  truths:
    - Catalog refresh reuses the already-established validation gate and last-known-good swap behavior instead of inventing a second cutover path.
    - A failed refresh leaves the last known-good SQLite catalog available instead of publishing broken state or silently falling back to CSV.
    - Repeated catalog-backed reads benefit from appropriate caching or invalidation without serving incorrect data after refresh.
  artifacts:
    - src/lib/catalog-import.ts
    - scripts/rebuild-catalog.ts
    - src/lib/__tests__/catalog-parity.test.ts
    - src/lib/__tests__/catalog-cache.test.ts
    - data/catalog/last-import-validation.json
  key_links:
    - Import validation and runtime cache invalidation use the same catalog metadata/version signals.
    - Sync/refresh automation stays on existing command and hook surfaces rather than introducing a new admin UI.
---

<objective>
Finish the Phase 2 cutover by adding import-time parity validation, safe refresh behavior, and lightweight caching or revalidation for repeated catalog reads.

Purpose: The SQLite swap should be fast, but it also needs an operational safety net. This plan hardens refresh and cache behavior so the app can trust SQLite as the only runtime browse source.
Output: Validation reports, atomic refresh semantics, correct cache invalidation, and documentation for the new catalog refresh path.
</objective>

<execution_context>
@/Users/ossieirondi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ossieirondi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sqlite-catalog/02-CONTEXT.md
@.planning/phases/02-sqlite-catalog/02-RESEARCH.md
@.planning/phases/02-sqlite-catalog/02-VALIDATION.md
@.planning/phases/02-sqlite-catalog/02-01-SUMMARY.md
@.planning/phases/02-sqlite-catalog/02-02-SUMMARY.md
@src/app/api/sync-hook/route.ts
@scripts/nightly-insights.ts
@docs/architecture/system-overview.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add parity and validation reporting to catalog refresh</name>
  <files>src/lib/catalog-import.ts, scripts/rebuild-catalog.ts, data/catalog/last-import-validation.json, src/lib/__tests__/catalog-parity.test.ts, src/lib/__tests__/catalog-import-validation.test.ts</files>
  <action>Extend the importer so it persists a machine-readable validation report covering canonical video counts, transcript-part counts, missing IDs, ordering mismatches, and malformed-row failures from the validation gate established in Plan 02-01. Keep this focused on durable reporting and operational visibility during refresh; do not reintroduce CSV as a runtime fallback source or duplicate the already-locked cutover rule.</action>
  <verify>npx vitest run src/lib/__tests__/catalog-parity.test.ts src/lib/__tests__/catalog-import-validation.test.ts && node scripts/rebuild-catalog.ts --check</verify>
  <done>Refresh can prove SQLite matches the expected browse behavior closely enough for cutover, and failed validations leave the last known-good DB untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Align runtime caching and refresh callers with the new catalog authority</name>
  <files>src/lib/catalog.ts, src/app/api/sync-hook/route.ts, scripts/nightly-insights.ts, src/lib/__tests__/catalog-cache.test.ts</files>
  <action>Implement lightweight cache invalidation or revalidation keyed to catalog import metadata or DB file replacement so repeated reads stay fast without serving stale or partial results. Update the sync hook and nightly workflows to use the explicit refresh path and the same catalog authority, including removing any hardcoded transcript-repo fallback behavior from `scripts/nightly-insights.ts` that would drift from the app runtime.</action>
  <verify>npx vitest run src/lib/__tests__/catalog-cache.test.ts src/lib/__tests__/catalog-parity.test.ts && npx eslint src/lib/catalog.ts src/app/api/sync-hook/route.ts scripts/nightly-insights.ts</verify>
  <done>Catalog-backed reads benefit from caching that invalidates correctly after refresh, and operational flows refresh SQLite instead of bypassing it.</done>
</task>

<task type="auto">
  <name>Task 3: Document the refresh and cutover contract</name>
  <files>README.md, docs/architecture/system-overview.md, scripts/rebuild-catalog.ts, src/lib/catalog.ts, src/app/api/sync-hook/route.ts, scripts/nightly-insights.ts</files>
  <action>Document where the SQLite catalog lives, how operators rebuild or validate it, what happens on validation failure, and which existing workflows now trigger refreshes. Keep the docs aligned with the private/internal deployment model and make it obvious that runtime browse reads are SQLite-only after Phase 2.</action>
  <verify>npx prettier --check README.md docs/architecture/system-overview.md && rg -n "catalog\\.db|rebuild-catalog|last-import-validation|videos\\.csv" README.md docs scripts src/lib/catalog.ts src/app/api/sync-hook/route.ts scripts/nightly-insights.ts</verify>
  <done>The code and docs tell one clear story about SQLite authority, refresh behavior, and failure handling.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/lib/__tests__/catalog-parity.test.ts src/lib/__tests__/catalog-import-validation.test.ts src/lib/__tests__/catalog-cache.test.ts`
- [ ] `node scripts/rebuild-catalog.ts --check`
- [ ] `npx eslint src/lib/catalog.ts src/app/api/sync-hook/route.ts scripts/nightly-insights.ts`
- [ ] `rg -n "catalog\\.db|last-import-validation|videos\\.csv" README.md docs scripts src/lib/catalog.ts src/app/api/sync-hook/route.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Import-time validation protects the live SQLite catalog from broken refreshes
- Runtime caching improves repeated catalog reads without serving incorrect data
- Operational flows and docs treat SQLite as the only runtime browse authority
  </success_criteria>

<output>
After completion, create `.planning/phases/02-sqlite-catalog/02-03-SUMMARY.md`
</output>
