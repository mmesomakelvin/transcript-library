---
phase: 02-sqlite-catalog
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - src/lib/catalog.ts
  - src/modules/catalog/index.ts
  - src/app/page.tsx
  - src/app/channels/page.tsx
  - src/app/channel/[channel]/page.tsx
  - src/app/video/[videoId]/page.tsx
  - src/app/api/channels/route.ts
  - src/app/api/channel/route.ts
  - src/app/api/video/route.ts
  - src/app/api/analyze/route.ts
  - src/app/api/sync-hook/route.ts
  - src/lib/__tests__/catalog-repository.test.ts
  - src/lib/__tests__/catalog-home-grouping.test.ts
  - src/lib/__tests__/catalog-transcript-order.test.ts
autonomous: true
requirements:
  - CAT-02
  - CAT-03
  - PERF-01
  - TEST-03
user_setup: []
must_haves:
  truths:
    - Home, channel, and video reads use SQLite-backed catalog queries instead of synchronous CSV parsing.
    - Public catalog helpers preserve current behavior for channel grouping, `videoId` lookup, and transcript-part ordering.
    - Browse surfaces and API routes keep their current shape while moving all hot-path metadata access onto the new catalog store.
  artifacts:
    - src/lib/catalog.ts
    - src/modules/catalog/index.ts
    - src/lib/__tests__/catalog-repository.test.ts
    - src/lib/__tests__/catalog-home-grouping.test.ts
    - src/lib/__tests__/catalog-transcript-order.test.ts
  key_links:
    - The module facade used by pages and routes still exports the same catalog entrypoints.
    - Query results match the old grouping/order semantics closely enough that UI behavior does not drift.
---

<objective>
Replace runtime browse reads with SQLite-backed repository calls while preserving the current app routes, page behavior, and transcript-path integration.

Purpose: Phase 2 only succeeds if the storage swap is invisible to users. This plan moves the hot request paths off CSV parsing without introducing a UI redesign or a second runtime authority.
Output: SQLite-backed catalog helpers powering home, channel, video, and related API routes with regression coverage for grouping and ordering behavior.
</objective>

<execution_context>
@/Users/ossieirondi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ossieirondi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sqlite-catalog/02-CONTEXT.md
@.planning/phases/02-sqlite-catalog/02-RESEARCH.md
@.planning/phases/02-sqlite-catalog/02-VALIDATION.md
@.planning/phases/02-sqlite-catalog/02-01-SUMMARY.md
@src/lib/catalog.ts
@src/modules/catalog/index.ts
@src/app/page.tsx
@src/app/channels/page.tsx
@src/app/channel/[channel]/page.tsx
@src/app/video/[videoId]/page.tsx
@src/app/api/sync-hook/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the catalog facade on top of SQLite repositories</name>
  <files>src/lib/catalog.ts, src/modules/catalog/index.ts, src/lib/__tests__/catalog-repository.test.ts, src/lib/__tests__/catalog-home-grouping.test.ts, src/lib/__tests__/catalog-transcript-order.test.ts</files>
  <action>Refactor `src/lib/catalog.ts` so its exported responsibilities stay stable but its backing data comes from SQLite repository queries rather than in-process CSV parsing. Preserve the canonical `Video` and `ChannelSummary` shapes, `absTranscriptPath()`, and the request-level memoization pattern where it still adds value. Add tests that prove `groupVideos()` home-page grouping semantics, `listChannels()`, `listVideosByChannel()`, `getVideo()`, and transcript-part ordering still match expected behavior against fixture data.</action>
  <verify>npx vitest run src/lib/__tests__/catalog-repository.test.ts src/lib/__tests__/catalog-home-grouping.test.ts src/lib/__tests__/catalog-transcript-order.test.ts</verify>
  <done>The shared catalog module is SQLite-backed, callers still import the same public functions, and repository-level parity tests for grouped browse behavior pass.</done>
</task>

<task type="auto">
  <name>Task 2: Cut browse surfaces and API routes over to the SQLite-backed catalog</name>
  <files>src/app/page.tsx, src/app/channels/page.tsx, src/app/channel/[channel]/page.tsx, src/app/video/[videoId]/page.tsx, src/app/api/channels/route.ts, src/app/api/channel/route.ts, src/app/api/video/route.ts, src/app/api/analyze/route.ts, src/app/api/sync-hook/route.ts</files>
  <action>Update any runtime assumptions in pages and routes that depended on the old in-memory CSV implementation so they use the new shared SQLite-backed facade cleanly. Preserve route params, page output shape, transcript-path reads, and home-page grouped browse behavior. Keep the sync hook aligned with the new catalog authority rather than reaching back to raw CSV helpers.</action>
  <verify>npx vitest run src/lib/__tests__/catalog-repository.test.ts src/lib/__tests__/catalog-home-grouping.test.ts src/lib/__tests__/catalog-transcript-order.test.ts && npm run build</verify>
  <done>Browse pages and API routes consume the SQLite-backed catalog successfully, and the app still builds with the new authority in place.</done>
</task>

<task type="auto">
  <name>Task 3: Trim CSV-era assumptions and confirm hot-path behavior stays simple</name>
  <files>src/lib/catalog.ts, src/modules/catalog/index.ts, src/app/page.tsx, src/app/channels/page.tsx, src/app/channel/[channel]/page.tsx, src/app/video/[videoId]/page.tsx, src/app/api/channels/route.ts, src/app/api/channel/route.ts, src/app/api/video/route.ts, src/app/api/analyze/route.ts, src/app/api/sync-hook/route.ts, src/lib/__tests__/catalog-repository.test.ts, src/lib/__tests__/catalog-home-grouping.test.ts, src/lib/__tests__/catalog-transcript-order.test.ts</files>
  <action>Remove leftover request-path assumptions that only made sense with CSV parsing, but do not redesign the public API. Keep comments sparse and focused on any non-obvious query or caching behavior. Make sure the final code no longer parses `videos.csv` during normal home/channel/video request handling, and that grouped browse behavior remains explicitly covered by tests rather than only inferred from page builds.</action>
  <verify>npx eslint src/lib/catalog.ts src/modules/catalog/index.ts src/app/page.tsx src/app/channels/page.tsx src/app/channel/[channel]/page.tsx src/app/video/[videoId]/page.tsx src/app/api/channels/route.ts src/app/api/channel/route.ts src/app/api/video/route.ts src/app/api/analyze/route.ts src/app/api/sync-hook/route.ts src/lib/__tests__/catalog-repository.test.ts src/lib/__tests__/catalog-home-grouping.test.ts src/lib/__tests__/catalog-transcript-order.test.ts && rg -n "videos\\.csv|readVideoRows|parseCsvLine" src/app src/lib src/modules</verify>
  <done>Normal browse requests no longer depend on CSV parsing, and the remaining catalog surface area is small enough for the final validation/caching plan to harden.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/lib/__tests__/catalog-repository.test.ts src/lib/__tests__/catalog-home-grouping.test.ts src/lib/__tests__/catalog-transcript-order.test.ts`
- [ ] `npm run build`
- [ ] `npx eslint src/lib/catalog.ts src/modules/catalog/index.ts src/app/page.tsx src/app/channels/page.tsx src/app/channel/[channel]/page.tsx src/app/video/[videoId]/page.tsx src/app/api/channels/route.ts src/app/api/channel/route.ts src/app/api/video/route.ts src/app/api/analyze/route.ts src/app/api/sync-hook/route.ts src/lib/__tests__/catalog-repository.test.ts src/lib/__tests__/catalog-home-grouping.test.ts src/lib/__tests__/catalog-transcript-order.test.ts`
- [ ] `rg -n "videos\\.csv|readVideoRows|parseCsvLine" src/app src/lib src/modules`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Home, channel, and video metadata reads are SQLite-backed
- Home-page grouping, channel grouping, and transcript-part ordering remain correct
- Normal request paths no longer parse the CSV index
  </success_criteria>

<output>
After completion, create `.planning/phases/02-sqlite-catalog/02-02-SUMMARY.md`
</output>
