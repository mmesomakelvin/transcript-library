---
phase: 02-sqlite-catalog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - vitest.config.ts
  - src/lib/catalog-db.ts
  - src/lib/catalog-import.ts
  - src/lib/catalog.ts
  - scripts/rebuild-catalog.ts
  - src/lib/__tests__/catalog-sqlite-import.test.ts
  - src/lib/__tests__/catalog-import-validation.test.ts
autonomous: true
requirements:
  - CAT-01
  - CAT-04
  - SAFE-04
  - TEST-03
user_setup: []
must_haves:
  truths:
    - Catalog metadata is imported into a SQLite database with one canonical row per `videoId` and ordered child rows for transcript parts.
    - The live catalog database path and ownership are resolved explicitly under the app-controlled data area rather than the transcript checkout or release tree.
    - Import-time validation rejects malformed or incomplete rows loudly enough that broken catalog state does not silently enter the runtime.
    - Validation runs before runtime cutover and blocks replacement of the live database when parity or schema checks fail.
    - The schema and import path are simple enough for later browse reads to switch to SQLite without preserving CSV parsing on hot paths.
  artifacts:
    - vitest.config.ts
    - src/lib/catalog-db.ts
    - src/lib/catalog-import.ts
    - scripts/rebuild-catalog.ts
    - src/lib/__tests__/catalog-sqlite-import.test.ts
    - src/lib/__tests__/catalog-import-validation.test.ts
  key_links:
    - SQLite schema, importer, and fixture-backed tests agree on canonical `videoId` and transcript-part ordering behavior.
    - The import script produces a complete validated catalog snapshot that later plans can query without reading `videos.csv` at request time.
    - The database-path helper and validation gate are both established before page and route cutover work begins.
---

<objective>
Lay down the SQLite catalog foundation for Phase 2 by adding the database dependency, schema/bootstrap logic, and a validated import pipeline from `videos.csv`.

Purpose: The rest of the phase depends on having a trustworthy SQLite snapshot that mirrors current catalog behavior. This plan creates that snapshot and proves the import path can reject bad source data before runtime reads depend on it.
Output: A rebuildable SQLite catalog database, import helpers, and fixture-backed tests for canonicalization and validation.
</objective>

<execution_context>
@/Users/ossieirondi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ossieirondi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-sqlite-catalog/02-CONTEXT.md
@.planning/phases/02-sqlite-catalog/02-RESEARCH.md
@.planning/phases/02-sqlite-catalog/02-VALIDATION.md
@src/lib/catalog.ts
@src/modules/catalog/index.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the SQLite runtime and schema bootstrap layer</name>
  <files>package.json, vitest.config.ts, src/lib/catalog-db.ts, src/lib/__tests__/catalog-sqlite-import.test.ts</files>
  <action>Add a stable SQLite dependency suitable for Node-based Next.js server code and local scripts. Create a small database helper module that owns database path resolution, connection bootstrap, schema creation, and indexes for the catalog tables. Lock the live DB location now: the default runtime path should be under the app-controlled data area (for example `data/catalog/catalog.db`), not in the transcript repo or release tree. In the same task, tighten `vitest.config.ts` if needed so catalog tests run cleanly without accidentally ingesting Playwright specs, and add initial bootstrap-focused test coverage that proves a catalog database can be opened and schema-created successfully.</action>
  <verify>npx vitest run src/lib/__tests__/catalog-sqlite-import.test.ts</verify>
  <done>The repo can open or initialize the catalog database through one shared helper, the DB path contract is explicit, the test harness is targeted correctly, and the dependency choice avoids experimental Node SQLite APIs.</done>
</task>

<task type="auto">
  <name>Task 2: Build the CSV-to-SQLite importer with explicit row validation</name>
  <files>src/lib/catalog-import.ts, src/lib/catalog.ts, scripts/rebuild-catalog.ts, src/lib/__tests__/catalog-sqlite-import.test.ts, src/lib/__tests__/catalog-import-validation.test.ts</files>
  <action>Create an importer that reads the existing `videos.csv`, canonicalizes rows by `parent_video_id || video_id`, preserves transcript-part ordering, applies the locked placeholder behavior for incomplete metadata, and writes into a temporary SQLite snapshot. Validate critical fields and duplicate/invalid part state loudly; do not let malformed catalog rows degrade silently into partial imports. Add the parity checks needed for cutover readiness here, including canonical video counts, part counts, and representative grouping/order comparisons, then only swap the temporary DB into the live location when those checks pass. Expose the importer through a script entrypoint that rebuilds the catalog explicitly instead of tying import to request-time reads.</action>
  <verify>npx vitest run src/lib/__tests__/catalog-sqlite-import.test.ts src/lib/__tests__/catalog-import-validation.test.ts</verify>
  <done>A rebuild command can generate a complete validated SQLite snapshot from fixture or real CSV input, and malformed or parity-failing input blocks the live DB swap.</done>
</task>

<task type="auto">
  <name>Task 3: Lock down the schema/import contract with targeted fixtures and comments</name>
  <files>vitest.config.ts, src/lib/catalog-db.ts, src/lib/catalog-import.ts, scripts/rebuild-catalog.ts, src/lib/__tests__/catalog-sqlite-import.test.ts, src/lib/__tests__/catalog-import-validation.test.ts</files>
  <action>Refine test fixtures and any necessary concise comments so future plans can rely on the import contract without re-deriving it. Cover at least single-part videos, multi-part videos, placeholder normalization, invalid-row rejection, and a failed-then-preserved live DB case. Keep comments brief and focused on canonicalization, DB ownership, or validation behavior that would otherwise be non-obvious.</action>
  <verify>npx eslint vitest.config.ts src/lib/catalog-db.ts src/lib/catalog-import.ts scripts/rebuild-catalog.ts src/lib/__tests__/catalog-sqlite-import.test.ts src/lib/__tests__/catalog-import-validation.test.ts && npx vitest run src/lib/__tests__/catalog-sqlite-import.test.ts src/lib/__tests__/catalog-import-validation.test.ts</verify>
  <done>The schema/import contract, DB path ownership, and validation gate are stable, tested, and readable enough for the query-swap plan to build on directly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/lib/__tests__/catalog-sqlite-import.test.ts src/lib/__tests__/catalog-import-validation.test.ts`
- [ ] `npx eslint vitest.config.ts src/lib/catalog-db.ts src/lib/catalog-import.ts scripts/rebuild-catalog.ts src/lib/__tests__/catalog-sqlite-import.test.ts src/lib/__tests__/catalog-import-validation.test.ts`
- [ ] `node scripts/rebuild-catalog.ts --check`
- [ ] `rg -n "catalog_videos|catalog_parts|source_csv_|catalog\\.db" src/lib scripts vitest.config.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- SQLite can be rebuilt from `videos.csv` through an explicit script path
- Import validation and parity checks reject malformed or drifting catalog state before cutover
- The live catalog DB path is explicit and app-owned
- The runtime now has a stable database foundation for Phase 2 caller swaps
  </success_criteria>

<output>
After completion, create `.planning/phases/02-sqlite-catalog/02-01-SUMMARY.md`
</output>
