---
phase: 03-durable-runtime
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - src/lib/analysis.ts
  - src/lib/insights.ts
  - src/lib/runtime-reconciliation.ts
  - src/lib/runtime-stream.ts
  - src/app/api/insight/route.ts
  - src/app/api/insight/stream/route.ts
  - src/lib/__tests__/runtime-reconciliation.test.ts
  - src/lib/__tests__/runtime-stream.test.ts
  - src/lib/__tests__/insight-stream-route.test.ts
autonomous: true
requirements:
  - RUN-03
  - PERF-03
  - TEST-04
user_setup: []
must_haves:
  truths:
    - When runtime metadata and published artifacts disagree, the system records an explicit mismatch state instead of presenting normal success.
    - Operators can tell which run is affected, when the mismatch was detected, and whether the next safe step is a clean rerun.
    - Live status delivery remains status-first with recent log tails, but avoids obviously wasteful duplicate polling work as concurrent viewers increase.
    - Insight routes expose reconciliation and live-status evidence clearly enough for downstream UI work to stay simple and trustworthy.
  artifacts:
    - src/lib/runtime-reconciliation.ts
    - src/lib/runtime-stream.ts
    - src/app/api/insight/route.ts
    - src/app/api/insight/stream/route.ts
    - src/lib/__tests__/runtime-reconciliation.test.ts
    - src/lib/__tests__/runtime-stream.test.ts
    - src/lib/__tests__/insight-stream-route.test.ts
  key_links:
    - Insight routes read reconciliation state produced from the same durable run authority established earlier in the phase.
    - Stream behavior is optimized around shared status snapshots and recent log tails rather than full-terminal polling loops per viewer.
---

<objective>
Finish the core runtime slice of Phase 3 by adding durable artifact reconciliation and less wasteful status/log streaming behavior.

Purpose: Durable run and batch records are not enough if the published artifact set can still drift silently or if live updates scale linearly with viewers. This plan closes the runtime trust gap by making mismatch state durable and by refitting the live stream around shared status-first snapshots.
Output: Reconciliation records, runtime stream helpers, updated insight routes, and regression coverage for mismatch detection and streaming behavior.
</objective>

<execution_context>
@/Users/ossieirondi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ossieirondi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-durable-runtime/03-CONTEXT.md
@.planning/phases/03-durable-runtime/03-RESEARCH.md
@.planning/phases/03-durable-runtime/03-VALIDATION.md
@.planning/phases/03-durable-runtime/03-01-SUMMARY.md
@.planning/phases/03-durable-runtime/03-02-SUMMARY.md
@src/lib/analysis.ts
@src/lib/insights.ts
@src/app/api/insight/route.ts
@src/app/api/insight/stream/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add durable artifact reconciliation records and mismatch detection</name>
  <files>src/lib/runtime-reconciliation.ts, src/lib/analysis.ts, src/lib/insights.ts, src/app/api/insight/route.ts, src/lib/__tests__/runtime-reconciliation.test.ts</files>
  <action>Create a reconciliation layer that inspects the latest durable run record, canonical analysis artifacts, structured analysis validity, compatibility status artifacts, and log evidence for each video. Persist a durable reconciliation record with explicit mismatch reason codes, timestamps, related `runId`, and resolution status. Route insight reads through this layer so mismatched state is surfaced as a warning or failure signal instead of silently appearing complete, and keep the first repair path focused on clean rerun readiness rather than automatic file surgery.</action>
  <verify>npx vitest run src/lib/__tests__/runtime-reconciliation.test.ts</verify>
  <done>Artifact mismatches become durable, queryable state with explicit reasons and rerun-ready guidance instead of ad hoc route behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Rework live status delivery around shared snapshots and recent log tails</name>
  <files>src/lib/runtime-stream.ts, src/app/api/insight/stream/route.ts, src/app/api/insight/route.ts, src/lib/insights.ts, src/lib/__tests__/runtime-stream.test.ts, src/lib/__tests__/insight-stream-route.test.ts</files>
  <action>Keep SSE as the transport, but replace the current per-connection disk polling loop with a shared status-first snapshot strategy keyed by `videoId`. The payload should emphasize named lifecycle stage, timestamps, current error state, reconciliation summary, and a recent log tail rather than a terminal-like full stream. Support restart-safe reconstruction from durable files, coarse heartbeats when nothing changes, and reduced duplicate work when several viewers watch the same analysis concurrently.</action>
  <verify>npx vitest run src/lib/__tests__/runtime-stream.test.ts src/lib/__tests__/insight-stream-route.test.ts</verify>
  <done>Live updates stay useful for operators while avoiding obviously wasteful per-viewer polling and redundant disk reads.</done>
</task>

<task type="auto">
  <name>Task 3: Tighten route payloads around reconciliation and stream evidence</name>
  <files>src/app/api/insight/route.ts, src/app/api/insight/stream/route.ts, src/lib/runtime-reconciliation.ts, src/lib/runtime-stream.ts, src/lib/__tests__/runtime-reconciliation.test.ts, src/lib/__tests__/runtime-stream.test.ts, src/lib/__tests__/insight-stream-route.test.ts</files>
  <action>Refine the route contracts so downstream UI work can consume reconciliation state, named lifecycle stages, recent log tails, and retry guidance without extra inference logic. Keep this slice runtime-focused: no workspace redesign and no documentation work in this plan.</action>
  <verify>npx eslint src/app/api/insight/route.ts src/app/api/insight/stream/route.ts src/lib/runtime-reconciliation.ts src/lib/runtime-stream.ts src/lib/__tests__/runtime-reconciliation.test.ts src/lib/__tests__/runtime-stream.test.ts src/lib/__tests__/insight-stream-route.test.ts && npx vitest run src/lib/__tests__/runtime-reconciliation.test.ts src/lib/__tests__/runtime-stream.test.ts src/lib/__tests__/insight-stream-route.test.ts</verify>
  <done>The runtime routes expose a stable operator-facing payload that the smaller follow-up UI/docs slice can consume directly.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/lib/__tests__/runtime-reconciliation.test.ts src/lib/__tests__/runtime-stream.test.ts src/lib/__tests__/insight-stream-route.test.ts`
- [ ] `npx eslint src/app/api/insight/route.ts src/app/api/insight/stream/route.ts src/lib/runtime-reconciliation.ts src/lib/runtime-stream.ts src/lib/__tests__/runtime-reconciliation.test.ts src/lib/__tests__/runtime-stream.test.ts src/lib/__tests__/insight-stream-route.test.ts`
- [ ] `rg -n "reconciliation|mismatch|heartbeat|recent log|EventSource|status-first" src/app/api/insight/route.ts src/app/api/insight/stream/route.ts src/lib`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Artifact mismatches are detectable, durably recorded, and exposed through runtime payloads instead of silent success
- Live status and log streaming avoid obviously wasteful per-viewer polling behavior while preserving useful operator feedback
- The runtime exposes reconciliation and stream data cleanly enough for a smaller follow-up UI/docs slice to stay thin
  </success_criteria>

<output>
After completion, create `.planning/phases/03-durable-runtime/03-03-SUMMARY.md`
</output>
