---
phase: 03-durable-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - src/app/api/sync-hook/route.ts
  - scripts/nightly-insights.ts
  - src/lib/catalog.ts
  - src/lib/runtime-batches.ts
  - src/modules/analysis/index.ts
  - src/lib/__tests__/runtime-batches.test.ts
  - src/lib/__tests__/sync-hook-route.test.ts
autonomous: true
requirements:
  - RUN-02
  - SAFE-02
  - TEST-04
user_setup: []
must_haves:
  truths:
    - A sync-hook or nightly pass leaves behind one durable batch record that operators can inspect to see what started, what stayed pending, what was skipped, and what failed.
    - Skipped work is explicit and reasoned, including cases like already analyzed or already running, instead of disappearing into a generic success response.
    - Duplicate or replayed webhook submissions are rejected, deduplicated, or reused clearly enough that operators do not mistake them for new work.
    - Sync-hook responses, nightly summaries, and durable JSON state tell the same truthful story about partial success and failure counts.
    - Batch visibility remains available through JSON artifacts and logs without requiring a new in-app admin surface.
  artifacts:
    - src/lib/runtime-batches.ts
    - src/app/api/sync-hook/route.ts
    - scripts/nightly-insights.ts
    - src/lib/__tests__/runtime-batches.test.ts
    - src/lib/__tests__/sync-hook-route.test.ts
  key_links:
    - Sync-hook and nightly both call the same batch creation and per-item transition helpers.
    - Per-video run authority from Plan 03-01 is reused instead of duplicated inside batch orchestration.
---

<objective>
Rework sync-hook and nightly/backfill execution around durable batch records, honest per-video outcomes, and safer webhook submission semantics.

Purpose: Hosted operators need truthful queue visibility, not fire-and-forget messages. This plan adds durable batch state and replay-resistant submission so automation can report what actually happened across a mixed worklist.
Output: A shared batch orchestration layer for sync-hook and nightly flows with per-item visibility, durable skip/failure accounting, and regression coverage for route semantics and abuse resistance.
</objective>

<execution_context>
@/Users/ossieirondi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ossieirondi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-durable-runtime/03-CONTEXT.md
@.planning/phases/03-durable-runtime/03-RESEARCH.md
@.planning/phases/03-durable-runtime/03-VALIDATION.md
@.planning/phases/03-durable-runtime/03-01-SUMMARY.md
@src/app/api/sync-hook/route.ts
@scripts/nightly-insights.ts
@src/lib/catalog.ts
@src/modules/analysis/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Introduce durable batch and batch-item records for sync and nightly work</name>
  <files>src/lib/runtime-batches.ts, src/app/api/sync-hook/route.ts, scripts/nightly-insights.ts, src/lib/__tests__/runtime-batches.test.ts</files>
  <action>Create a batch service that snapshots a candidate worklist, persists a durable batch record plus per-video child items, and records explicit counts or reasons for queued, started, pending, skipped, failed, and completed work. The batch model should reuse the durable per-video latest-run authority from Plan 03-01 instead of inventing a second status system. Replace the stale nightly queue/worker-shell flow with the same batch submission path used by the webhook and keep additive filesystem artifacts compatible with future worker separation.</action>
  <verify>npx vitest run src/lib/__tests__/runtime-batches.test.ts</verify>
  <done>Sync-hook and nightly work now share one durable batch model that truthfully captures partial outcomes and explicit skip reasons.</done>
</task>

<task type="auto">
  <name>Task 2: Harden sync-hook submission semantics with dedupe and replay resistance</name>
  <files>src/app/api/sync-hook/route.ts, src/lib/runtime-batches.ts, src/lib/__tests__/runtime-batches.test.ts, src/lib/__tests__/sync-hook-route.test.ts</files>
  <action>Keep the existing bearer-token baseline, but add request identity or equivalent durable dedupe so repeated webhook submissions cannot silently create misleading duplicate batches. Record request metadata in a way that supports private hosted operations, idempotent retries, and clearer route responses. Preserve the route's ability to return quickly, but make its JSON payload reflect the batch that was created, reused, rejected, or rate-limited instead of always returning a generic success message.</action>
  <verify>npx vitest run src/lib/__tests__/runtime-batches.test.ts src/lib/__tests__/sync-hook-route.test.ts</verify>
  <done>The sync hook can reject or reuse duplicate submissions safely, and its response payload now points to a truthful durable batch outcome.</done>
</task>

<task type="auto">
  <name>Task 3: Align batch responses, logs, and automation callers with honest outcome reporting</name>
  <files>src/app/api/sync-hook/route.ts, scripts/nightly-insights.ts, src/lib/runtime-batches.ts, src/modules/analysis/index.ts, src/lib/__tests__/runtime-batches.test.ts, src/lib/__tests__/sync-hook-route.test.ts</files>
  <action>Update automation-facing responses and summaries so operators can see what started, what stayed pending, what was skipped, and what failed without reading source code. Keep route and script outputs machine-readable and log-friendly, and ensure candidate selection continues to respect the canonical catalog plus durable per-video analysis state. Avoid adding a new UI surface; the deliverable is trustworthy JSON state, script output, and tests.</action>
  <verify>npx eslint src/app/api/sync-hook/route.ts scripts/nightly-insights.ts src/lib/runtime-batches.ts src/modules/analysis/index.ts src/lib/__tests__/runtime-batches.test.ts src/lib/__tests__/sync-hook-route.test.ts && npx vitest run src/lib/__tests__/runtime-batches.test.ts src/lib/__tests__/sync-hook-route.test.ts</verify>
  <done>Automation and operators can inspect durable batch artifacts or responses and see honest partial outcomes instead of a flattened success story.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/lib/__tests__/runtime-batches.test.ts src/lib/__tests__/sync-hook-route.test.ts`
- [ ] `npx eslint src/app/api/sync-hook/route.ts scripts/nightly-insights.ts src/lib/runtime-batches.ts src/modules/analysis/index.ts src/lib/__tests__/runtime-batches.test.ts src/lib/__tests__/sync-hook-route.test.ts`
- [ ] `rg -n "batch|pending|skipped|duplicate|replay|idempot" src/app/api/sync-hook/route.ts scripts/nightly-insights.ts src/lib/runtime-batches.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Sync-hook and nightly/backfill flows report durable batch outcomes with explicit item-level reasons
- Webhook-triggered batch submission has meaningful duplicate or replay protection appropriate for the current private deployment
- Operators can understand partial success, skipped work, and failures from batch artifacts, route responses, and logs
  </success_criteria>

<output>
After completion, create `.planning/phases/03-durable-runtime/03-02-SUMMARY.md`
</output>
