---
phase: 03-durable-runtime
plan: 04
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - src/components/VideoAnalysisWorkspace.tsx
  - src/app/api/insight/route.ts
  - src/app/api/insight/stream/route.ts
  - README.md
  - docs/architecture/system-overview.md
  - src/lib/__tests__/insight-stream-route.test.ts
autonomous: true
requirements:
  - RUN-04
  - TEST-04
user_setup: []
must_haves:
  truths:
    - The existing video workspace shows mismatch warnings, retry guidance, and status-first runtime evidence clearly enough that operators do not mistake broken artifacts for success.
    - Recent useful log output is visible without turning the page into a terminal-like admin surface, and full logs remain secondary.
    - The documented observability contract explains how `status.json`, `run.json`, worker logs, and live status reads relate so future runtime changes do not create operator confusion.
    - UI and docs stay aligned with the current private, desktop-first product direction rather than expanding Phase 3 into a broader redesign.
  artifacts:
    - src/components/VideoAnalysisWorkspace.tsx
    - src/app/api/insight/route.ts
    - src/app/api/insight/stream/route.ts
    - README.md
    - docs/architecture/system-overview.md
    - src/lib/__tests__/insight-stream-route.test.ts
  key_links:
    - The UI consumes reconciliation and stream payloads produced by Plan 03-03 instead of creating a second observability model.
    - Docs describe the same operator-visible behavior that the routes and workspace actually expose.
---

<objective>
Finish the operator-facing edge of Phase 3 by surfacing reconciliation and live-status behavior clearly in the existing workspace and by documenting the observability contract.

Purpose: The runtime changes from earlier plans are only trustworthy if the existing UI and docs present them clearly. This plan keeps that work separate from the core runtime slice so execution stays smaller and easier to verify.
Output: A clearer video analysis workspace, aligned route payload usage, and documentation for the runtime observability contract.
</objective>

<execution_context>
@/Users/ossieirondi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ossieirondi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-durable-runtime/03-CONTEXT.md
@.planning/phases/03-durable-runtime/03-VALIDATION.md
@.planning/phases/03-durable-runtime/03-03-SUMMARY.md
@src/components/VideoAnalysisWorkspace.tsx
@src/app/api/insight/route.ts
@src/app/api/insight/stream/route.ts
@README.md
@docs/architecture/system-overview.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Surface reconciliation warnings and retry guidance in the existing workspace</name>
  <files>src/components/VideoAnalysisWorkspace.tsx, src/app/api/insight/route.ts, src/app/api/insight/stream/route.ts, src/lib/__tests__/insight-stream-route.test.ts</files>
  <action>Update the existing analysis workspace to show clearer mismatch warnings, retry guidance, and status-first live evidence using the reconciliation and stream payloads established in Plan 03-03. Keep the page aligned with the current desktop-first product direction and avoid turning it into a new admin console.</action>
  <verify>npx vitest run src/lib/__tests__/insight-stream-route.test.ts</verify>
  <done>The video page makes mismatches and retry paths obvious without requiring operators to inspect raw files first.</done>
</task>

<task type="auto">
  <name>Task 2: Keep recent logs secondary and useful</name>
  <files>src/components/VideoAnalysisWorkspace.tsx, src/app/api/insight/stream/route.ts, src/lib/__tests__/insight-stream-route.test.ts</files>
  <action>Present only the most recent useful runtime evidence by default, keep full logs secondary, and preserve the status-first operator experience chosen in the phase context. Do not expand this into a broader UI redesign.</action>
  <verify>npx eslint src/components/VideoAnalysisWorkspace.tsx src/app/api/insight/stream/route.ts src/lib/__tests__/insight-stream-route.test.ts && npx vitest run src/lib/__tests__/insight-stream-route.test.ts</verify>
  <done>The live workspace remains operator-friendly and focused on actionable state rather than noisy terminal output.</done>
</task>

<task type="auto">
  <name>Task 3: Document the observability contract for future runtime changes</name>
  <files>README.md, docs/architecture/system-overview.md, src/app/api/insight/route.ts, src/app/api/insight/stream/route.ts</files>
  <action>Document how `status.json`, `run.json`, worker logs, reconciliation state, and live status payloads fit together in the private hosted runtime. Keep the docs concise, operational, and aligned with the actual route behavior from the earlier runtime plans.</action>
  <verify>npx prettier --check README.md docs/architecture/system-overview.md && rg -n "status\\.json|run\\.json|reconciliation|worker log|live status" README.md docs/architecture/system-overview.md src/app/api/insight/route.ts src/app/api/insight/stream/route.ts</verify>
  <done>The repo documentation tells the same operator-facing story as the runtime and UI.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/lib/__tests__/insight-stream-route.test.ts`
- [ ] `npx eslint src/components/VideoAnalysisWorkspace.tsx src/app/api/insight/stream/route.ts src/lib/__tests__/insight-stream-route.test.ts`
- [ ] `npx prettier --check README.md docs/architecture/system-overview.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Operators can see mismatch warnings, retry guidance, and recent runtime evidence clearly in the existing workspace
- Full logs stay secondary to status-first operator feedback
- Documentation explains the observability contract without drifting from implemented behavior
  </success_criteria>

<output>
After completion, create `.planning/phases/03-durable-runtime/03-04-SUMMARY.md`
</output>
