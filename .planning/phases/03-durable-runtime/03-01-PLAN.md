---
phase: 03-durable-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/analysis.ts
  - src/modules/analysis/index.ts
  - src/app/api/analyze/route.ts
  - src/app/api/analyze/status/route.ts
  - src/app/api/insight/route.ts
  - src/app/api/insight/stream/route.ts
  - src/lib/insights.ts
  - src/lib/__tests__/runtime-runs.test.ts
  - src/lib/__tests__/runtime-compat.test.ts
autonomous: true
requirements:
  - RUN-01
  - RUN-04
  - TEST-04
user_setup: []
must_haves:
  truths:
    - After a restart, operators can open a video and see a clear interrupted, failed, running, or completed state rather than an ambiguous stale "running" badge.
    - For any analysis attempt, the latest visible run record tells operators when it started, whether it finished, and why it failed or was interrupted without requiring process inspection.
    - Existing `status.json`, `run.json`, and worker log files remain available as trustworthy operator evidence instead of disagreeing about the last known outcome.
    - If a completed analysis already exists, a rerun attempt produces an explicit already-analyzed or retry-needed outcome rather than silently replacing evidence.
    - Manual analyze, status APIs, and insight reads all present the same last-known run truth for a given `videoId`.
  artifacts:
    - src/lib/analysis.ts
    - src/modules/analysis/index.ts
    - src/lib/__tests__/runtime-runs.test.ts
    - src/lib/__tests__/runtime-compat.test.ts
  key_links:
    - Manual analyze, status reads, and insight reads all consult the same latest-run authority.
    - Compatibility artifacts stay readable for operators and later plans without preserving the old split-brain lifecycle rules.
---

<objective>
Establish the durable run-state foundation for Phase 3 by introducing attempt-aware analysis records, shared lifecycle transitions, and restart-safe status reconciliation.

Purpose: The rest of the phase depends on one trustworthy runtime authority. This plan converts analysis execution from a mostly in-memory/PID-derived model into a durable latest-run contract that routes, scripts, and the UI can all trust after restarts.
Output: A shared runtime lifecycle layer with durable latest-run metadata, compatibility-mapped `status.json`/`run.json`, and regression coverage for transitions, interruptions, and rerun policy.
</objective>

<execution_context>
@/Users/ossieirondi/.codex/get-shit-done/workflows/execute-plan.md
@/Users/ossieirondi/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-durable-runtime/03-CONTEXT.md
@.planning/phases/03-durable-runtime/03-RESEARCH.md
@.planning/phases/03-durable-runtime/03-VALIDATION.md
@src/lib/analysis.ts
@src/modules/analysis/index.ts
@src/app/api/analyze/route.ts
@src/app/api/analyze/status/route.ts
@src/app/api/insight/route.ts
@src/app/api/insight/stream/route.ts
@src/lib/insights.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create a durable latest-run authority and attempt identity model</name>
  <files>src/lib/analysis.ts, src/modules/analysis/index.ts, src/lib/__tests__/runtime-runs.test.ts</files>
  <action>Refactor the analysis runtime so one transition layer owns durable lifecycle state for each `videoId`. Introduce a `runId`, richer lifecycle vocabulary that can express queued, running, completed, failed, interrupted, and reconciled outcomes, and helpers that write latest-run metadata deterministically under the existing insight directory. Preserve the per-video root artifact layout, but separate attempt identity from the published canonical artifact names so later reconciliation and rerun work does not erase evidence. Keep provider switching behind the runtime boundary and avoid introducing UI-facing provider logic.</action>
  <verify>npx vitest run src/lib/__tests__/runtime-runs.test.ts</verify>
  <done>The runtime exposes one durable latest-run record per video with explicit attempt identity and lifecycle semantics that later routes and plans can reuse directly.</done>
</task>

<task type="auto">
  <name>Task 2: Map compatibility artifacts and restart reconciliation onto the shared transition path</name>
  <files>src/lib/analysis.ts, src/modules/analysis/index.ts, src/app/api/analyze/status/route.ts, src/app/api/insight/route.ts, src/app/api/insight/stream/route.ts, src/lib/insights.ts, src/lib/__tests__/runtime-compat.test.ts</files>
  <action>Make `run.json` the durable latest-run authority and derive `status.json`, log-path continuity, and any route-level status snapshots from the same transition helpers. Remove route-local stale-process patching as the source of truth; instead, centralize restart reconciliation so a previously running process becomes an explicit interrupted or failed state when no live worker remains. Preserve current operator artifact names and readable payloads required by `RUN-04`, including worker stdout/stderr continuity and stable route response shapes where feasible.</action>
  <verify>npx vitest run src/lib/__tests__/runtime-runs.test.ts src/lib/__tests__/runtime-compat.test.ts</verify>
  <done>Status reads, SSE snapshots, and insight reads no longer invent their own lifecycle semantics, and compatibility artifacts remain available through one write path.</done>
</task>

<task type="auto">
  <name>Task 3: Harden analyze-start and rerun policy with regression coverage</name>
  <files>src/lib/analysis.ts, src/app/api/analyze/route.ts, src/app/api/analyze/status/route.ts, src/app/api/insight/route.ts, src/lib/__tests__/runtime-runs.test.ts, src/lib/__tests__/runtime-compat.test.ts</files>
  <action>Update manual analyze entrypoints to use the new lifecycle helpers for queue/start/fail/success transitions, explicit duplicate-run prevention, and rerun messaging when canonical analysis artifacts already exist. Keep the UI contract desktop-first and stable, but ensure routes can distinguish already-complete, already-running, interrupted, and retryable states cleanly. Add concise comments only where run-authority or compatibility behavior would otherwise be hard to infer.</action>
  <verify>npx eslint src/lib/analysis.ts src/modules/analysis/index.ts src/app/api/analyze/route.ts src/app/api/analyze/status/route.ts src/app/api/insight/route.ts src/app/api/insight/stream/route.ts src/lib/insights.ts src/lib/__tests__/runtime-runs.test.ts src/lib/__tests__/runtime-compat.test.ts && npx vitest run src/lib/__tests__/runtime-runs.test.ts src/lib/__tests__/runtime-compat.test.ts</verify>
  <done>Manual analysis start and status behavior now flow through the durable lifecycle model with tests proving restart-safe and rerun-safe behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npx vitest run src/lib/__tests__/runtime-runs.test.ts src/lib/__tests__/runtime-compat.test.ts`
- [ ] `npx eslint src/lib/analysis.ts src/modules/analysis/index.ts src/app/api/analyze/route.ts src/app/api/analyze/status/route.ts src/app/api/insight/route.ts src/app/api/insight/stream/route.ts src/lib/insights.ts src/lib/__tests__/runtime-runs.test.ts src/lib/__tests__/runtime-compat.test.ts`
- [ ] `rg -n "runId|interrupted|reconciled|status\\.json|run\\.json" src/lib src/app/api`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Run state survives process restarts without relying only on in-memory counters or route-local PID logic
- `status.json`, `run.json`, and worker logs remain available through one compatibility-mapped runtime authority
- Manual analyze flows can distinguish already-running, already-complete, interrupted, failed, and retryable states clearly
  </success_criteria>

<output>
After completion, create `.planning/phases/03-durable-runtime/03-01-SUMMARY.md`
</output>
